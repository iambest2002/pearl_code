<view class="words-button">
<van-button class="cet4-button" color="#96e3fa" bind:tap="goToCet4WordsPage" type="primary" square >CET4</van-button>
  <van-button class="cet6-button" color="#96e3fa" bind:tap="goToCet6WordsPage" type="primary" square >CET6</van-button>
  <van-button class="pgee-button" color="#96e3fa" bind:tap="goToPgeeWordsPage" type="primary" square >PGEE</van-button>
  <van-button class="pgee-button" color="#96e3fa" bind:tap="goToWordsBookPage" type="primary" square >单词本</van-button>
</view>
<!-- <van-tabs sticky active="{{ active }}" bind:change="onChange">
  <van-tab title="标签 1">内容 1</van-tab>
  <van-tab title="标签 2">内容 2</van-tab>
  <van-tab title="标签 3">内容 3</van-tab>
  <van-tab title="标签 4">内容 4</van-tab>
</van-tabs> -->

<van-search disabled value="{{ searchValue }}" placeholder="请输入搜索关键词" show-action="true" use-action-slot bind:change="onChangeSearch" bind:cancel="onCancel" bind:search="onSearch" class="search-bar">
    <!-- 自定义“搜索”按钮 -->
    <view slot="action" bind:tap="onClick">
      <image class="search-icon" src="../../image/search.png"></image>
    </view>
  </van-search>

  
<!-- <van-popup custom-style="height: 40%;" show="{{ goToCet4WordsPicker }}" round position="bottom"  custom-style="height: 40%;" >
    <van-picker
  show-toolbar
  title="获取单词缓存"
  columns="{{ goToWordsaPage }}"
  bind:cancel="goToCet4WordsPickerCancel"
  bind:confirm="goToCet4WordsConfirm"
/>
</van-popup> -->


<!-- 
<view class="center-box">
  <view class="nav">
    <view class="left {{current==0?'select':''}}" bindtap="click" data-code="0">
      <text class="NavigationBar-text">CET4</text>
    </view>
    <view plain color="#fffff" class="right {{current==1?'select':''}}" bindtap="click" data-code="1">
      <text class="NavigationBar-text">CET6</text>
    </view>
    <view class="left {{current==2?'select':''}}" bindtap="click" data-code="2">
      <text class="NavigationBar-text">PGEE</text>
    </view>
  </view>

<view class="container" hidden="{{current!=0}}">
<view class="options">
  <van-index-bar sticky highlight-color="#96e3fa">
    <block wx:for="{{alphabet}}" wx:key="_id">
      <view wx:if="{{cet4WordsGrouped[item]}}">
        <van-index-anchor index="{{item}}" />
        <block wx:for="{{cet4WordsGrouped[item]}}" wx:key="_id">
          <view class="word-box">
            <text class="word">{{item.word}}</text>
            <block wx:for="{{item.translations}}" wx:key="_id">
              <text class="translation">{{item.translation}} ({{item.type}})</text>
            </block>
          </view>
        </block>
      </view>
    </block>
  </van-index-bar> -->

  <!-- 加载更多按钮 -->
  <!-- <view wx:if="{{hasMore}}" class="load-more">
    <button bindtap="loadWordsBatch" loading="{{loading}}" disabled="{{loading}}">
      {{loading ? '加载中...' : '加载更多'}}
    </button>
  </view> -->

  <!-- 没有更多数据的提示 -->
  <!-- <view wx:if="{{!hasMore}}" class="no-more">
    <text>没有更多数据了</text>
  </view>
</view>
</view> -->

<!-- <view class="container" hidden="{{current!=1}}">
<view class="options">
  <van-index-bar sticky highlight-color="#96e3fa">
    <block wx:for="{{alphabet}}" wx:key="_id">
      <view wx:if="{{cet6WordsGrouped[item]}}">
        <van-index-anchor index="{{item}}" />
        <block wx:for="{{cet6WordsGrouped[item]}}" wx:key="_id">
          <view class="word-box">
            <text class="word">{{item.word}}</text>
            <block wx:for="{{item.translations}}" wx:key="_id">
              <text class="translation">{{item.translation}} ({{item.type}})</text>
            </block>
          </view>
        </block>
      </view>
    </block>
  </van-index-bar> -->

  <!-- 加载更多按钮 -->
  <!-- <view wx:if="{{hasMore}}" class="load-more">
    <button bindtap="loadWordsBatch" loading="{{loading}}" disabled="{{loading}}">
      {{loading ? '加载中...' : '加载更多'}}
    </button>
  </view> -->

  <!-- 没有更多数据的提示 -->
  <!-- <view wx:if="{{!hasMore}}" class="no-more">
    <text>没有更多数据了</text>
  </view>
</view>
</view> -->

<!-- <view class="container" hidden="{{current!=2}}">
<view class="options">
  <van-index-bar sticky highlight-color="#96e3fa">
    <block wx:for="{{alphabet}}" wx:key="_id">
      <view wx:if="{{pgeeWordsGrouped[item]}}">
        <van-index-anchor index="{{item}}" />
        <block wx:for="{{pgeeWordsGrouped[item]}}" wx:key="_id">
          <view class="word-box">
            <text class="word">{{item.word}}</text>
            <block wx:for="{{item.translations}}" wx:key="_id">
              <text class="translation">{{item.translation}} ({{item.type}})</text>
            </block>
          </view>
        </block>
      </view>
    </block>
  </van-index-bar> -->

  <!-- 加载更多按钮 -->
  <!-- <view wx:if="{{hasMore}}" class="load-more">
    <button bindtap="loadWordsBatch" loading="{{loading}}" disabled="{{loading}}">
      {{loading ? '加载中...' : '加载更多'}}
    </button>
  </view> -->

  <!-- 没有更多数据的提示 -->
  <!-- <view wx:if="{{!hasMore}}" class="no-more">
    <text>没有更多数据了</text>
  </view>
</view>
</view> -->
<!-- </view>
<view class="scroll-buttons-container"> -->
  <!-- 一键上滑按钮 -->
  <!-- <view class="scroll-button-up" bindtap="scrollToTop">
    <image class="button-icon" src="../../image/up.png"></image>
  </view> -->

  <!-- 一键下滑按钮 -->      
  <!-- <view class="scroll-button" bindtap="scrollToBottom">
    <image class="button-icon" src="../../image/down.png"></image>
  </view>
</view> -->